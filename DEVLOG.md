# 베지왕 개발일지

## Chapter 1 — 가격 축 정규화와 기준점 확립

*2026-07-31*

### 목표

Vercel·GitHub에 배포된 베지왕의 **올바른 DB**와, 제공받은 가격(가락 경락가 ·
KAMIS 도소매)에 대한 **가격기준점**을 제대로 세운다.

### 발견한 문제 (프로덕션 실측)

배포된 서비스가 아래 값을 내보내고 있었다.

| 품목 | 노출 경락가 | 소매가 | 거품배수 |
|---|---:|---:|---:|
| 무 | 36원/kg | 1,050 | 29.2배 |
| 양파 | 73원/kg | 1,876 | 25.7배 |
| 피망 | 169원/kg | 10,925 | **64.6배** |

원인은 셋이 겹친 것이었다.

1. **DB가 존재하지 않았다.** `/api/health`가 `databaseConfigured: false`,
   `storage: "memory"`. `db/migrations/001,002`는 작성만 되고 적용된 적이 없었다.
   `raw_auction` 적재량 0 → 자체 평년선(`item_baseline`)이 생성될 수 없는 구조.
2. **경락가가 weightKg만큼 두 번 나눠졌다.** KAMIS `p_convert_kg_yn=Y`가 dpr1~dpr4를
   원/kg로 내려주는데 거래단위 가격으로 취급했고, `reconcileAuctionPrice`가 두 값이
   3.5배 이상 벌어지면 낮은 쪽을 채택해 **정상값인 가락 상자가를 항상 버렸다.**
   그 원/kg 값이 `withSignal`에서 다시 weightKg로 나눠졌다.
3. **KAMIS 시계열에 두 축이 섞였다.** 같은 행에서 dpr2=1,128(원/kg)과
   dpr7=13,146(원/10kg)을 한 시계열로 합치고 있었다. 편차율이 -89%~+218%로 널뛴 이유.

부수적으로 하드코딩 가격 더미 20여 품목이 표시 없이 실시세인 척 노출됐고
(아보카도 20,000원/kg, 조기 66,667원/kg), 품목명 매칭이 양방향 부분문자열이라
"배"가 "배추"·"양배추"에 걸렸다.

### 확정한 축 규칙

배추 실측 교차검증으로 확정 (`unit="10kg(그물망 3포기)"`):

| 값 | 원문 | 원/kg |
|---|---:|---:|
| 가락 경락가 (UUN 기반) | 1,895 | 1,895 |
| KAMIS dpr2 (1일전) | 1,128 | 1,128 |
| KAMIS dpr7 (평년) | 13,146 | 1,315 (÷10kg) |

→ **`p_convert_kg_yn=Y`는 중량 기반 단위의 dpr1~dpr4만 변환한다.**
dpr5·dpr6·dpr7과 개수 기반 단위("1포기","10개")는 변환되지 않는다.
시금치 소매 "100g" dpr2, 배추 소매 "1포기" dpr2로 교차 확인.

### 완료 내용

- **단일 정규화 지점** — 내부 표준축을 원/kg 하나로. 나눗셈은 소스 어댑터 안에서만,
  이후로는 곱하기만 한다. 타입 이름에 축을 새겨 컴파일러가 강제하도록 함
  (`auctionPerKg`, `avgPricePerKg`, `baselinePerKg`).
- **축 해석기** `resolveKamisPerKg(slot, value, unit, kgPerPiece)` + 회귀 테스트.
  환산 근거가 없으면 `null` — 1kg으로 가정하지 않는다.
- **축 게이트** `resolveAuctionPerKg` — 두 소스가 10배 이상 벌어지면 낮은 쪽을
  고르는 대신 둘 다 거부하고 진단 로그를 남긴다.
- **카탈로그 검증** `scripts/verify-catalog.mjs` — KAMIS 실응답 단위와 대조.
  **47/56 통과**, 미통과 9종은 비노출. 근거는 `docs/CATALOG_VERIFICATION.md`.
- **하드코딩 더미 전량 제거** — `sample-data.ts` 삭제, 카탈로그는 메타 전용.
- **결측 정책** — 최근 7일 실측 이월 + 날짜 라벨, 그 밖은 비노출.
  소매가가 없으면 거품 배수·절약액을 표시하지 않는다(타입에서 optional).
- **기준선 근거 추적** — `baselineMethod`(`kamis_dpr7`/`moving_avg_30`/`seasonal`).
  자체 이력 14일 미만이면 이동평균인 척하지 않는다.
- **마이그레이션 003** — 원/kg 컬럼, `price_status`, `items.unit_verified`,
  `daily_item_price_served` 뷰. 원문 거래단위 컬럼은 재집계용으로 보존.
- **마이그레이션 검증 테스트** — PGlite(실제 Postgres WASM)에 001~003 전량 적용 +
  멱등 재실행 + CHECK 제약·뷰 동작 검증.
- **진단 엔드포인트** `/api/debug/price-axis`, `/api/health`에 `lastIngest`·카탈로그 요약.

### 검증

- 타입체크·린트·프로덕션 빌드 통과
- 테스트 **89개 통과** (14개 파일)
- 로컬 실행(키 없는 상태): `/api/prices` → **items 0개**
  (예전에는 56개 전부 더미로 채워졌다). 홈·카테고리·상세 모두 200.

### 다음 할 일

1. **DB 실가동 (사용자 조치 필요)** — Vercel Project Settings에 등록:
   ```
   DATABASE_URL=postgresql://postgres.[ref]:[PW]@…pooler.supabase.com:5432/postgres
   CRON_SECRET=<openssl rand -hex 32>
   ```
   이후 `npm run db:migrate` → Cron(08:00 KST) 실가동 → `raw_auction` 적재 확인.
   ※ 배포된 Vercel 프로젝트가 `seoul-rave` 팀에 없어 에이전트가 대신 설정할 수 없음.
2. 배포 후 `/api/debug/price-axis`로 거품배수 분포 확인 (정상이면 대략 1.2~6배)
3. 자체 이력 14일 축적 후 기준선이 `moving_avg_30`으로 전환되는지 확인
4. 축 정상화 후 거품 임계값(1.8 / 2.5) 실측 분포로 재조정
5. 미검증 9품목 처리 — 수박(가락 UUN으로 실중량 확인),
   수산 7종(도매 원천 부재 → 해수부 위판장 API 필요)

---

## Chapter 2 — 수산 경락가 원천 연결 (해수부 위판장)

*2026-07-31*

### 목표

Chapter 1에서 수산 대부분이 카탈로그에서 빠졌다. 가락은 청과 6개 법인만
조회하므로 수산 경락가 원천이 애초에 없었고, 그 자리를 더미가 메우고 있었다.
해양수산부 위판장 API를 붙여 산지 위판가를 진짜 원천으로 삼는다.

### 조사 결과

명세를 추측하지 않고 공공데이터포털에서 확인했다.

| 항목 | 값 |
|---|---|
| API | 해양수산부_위판장별 위탁판매 현황 (15056856) |
| 엔드포인트 | `apis.data.go.kr/1192000/select0040List/getselect0040List` |
| 인증 | `DATA_GO_KR_SERVICE_KEY` (aT와 같은 키) |
| 핵심 필드 | `csmtAmount`(금액) · `csmtWt`(중량) · `csmtUntpc`(단가) · `goodsUnitNm`(단위) |

같은 계열의 `select0050List`(어종별 집계)도 봤지만 단위가 `상자(C/S)`로
내려오고 중량 필드가 없어 KAMIS와 같은 축 문제가 생긴다. 채택하지 않았다.

### 축 결정

**원/kg = csmtAmount ÷ csmtWt.**

단위 문자열을 전혀 파싱하지 않는 자기완결적 환산이다. 가락은 UUN을
파싱해야 하고 KAMIS는 슬롯마다 축이 달랐지만, 여기는 금액과 중량이 같은
행에 있어 나눗셈 한 번으로 축이 확정된다.

`csmtUntpc`는 상자/마리 기준이 섞여 있어 대표값으로 쓰지 않는다. 대신
`단가 ÷ (금액÷중량)` 비율을 남겨 축 진단에 쓴다 — 1에 가까우면 단가도 원/kg.

### 완료 내용

- `fishMarket.ts` 어댑터 + 단위 테스트 11개. 전국 위판장 총금액÷총중량
  중량 가중평균, 신선/냉장 우선(냉동과 섞지 않음)
- `sourceMarketFor()`로 원천 분리 — 청과는 가락, 수산은 위판장.
  `PriceItem.sourceMarket`으로 카드마다 출처 표시
- **수산에는 KAMIS 도매 시계열·평년가를 쓰지 않는다.** 도매시장가와 산지
  위판가는 유통 단계가 달라 섞으면 산지가 항상 '고가권'으로 보인다
- 마이그레이션 004: `raw_auction.source`에 `fish_market` 허용,
  시장 `900001` "전국 수협 위판장", `items.source_market`
- 수집·서빙 모두 두 시장을 읽는다. 위판 수집 실패가 청과를 막지 않는다
- `/api/debug/fish-market` — 키가 붙는 순간 축을 확정하는 진단 경로

### 카탈로그 47 → 53 / 56

수산의 원/kg가 금액÷중량으로 나오므로 거래단위 환산 자체가 불필요하다.
수산에 KAMIS 도매 단위 대조를 요구하던 규칙을 제거했다.

출처 없는 '1마리 중량'은 지어내지 않았다. 꽁치·조기·수입조기는 거래단위를
위판 원천 축(중량)에 맞춰 1kg으로 통일하고 마리 단위 주장을 제거했다.

남은 3종: 수박(도매 단위가 개수 기반), 아보카도(도매 시세 없음),
수입조기(수입산 — 국내 산지 위판 원천 없음).

### 검증

- 테스트 **103개 통과** (15개 파일)
- 마이그레이션 001~004 PGlite 실제 Postgres 적용 + 멱등 확인
- 타입체크·린트·프로덕션 빌드 통과
- 키 없는 로컬 실행: `/api/debug/fish-market` → `missing_credentials` 명시,
  피드는 여전히 items 0개(더미 없음), 홈·수산 카테고리·상세 모두 200

### ⚠ 아직 확인 못 한 것

`DATA_GO_KR_SERVICE_KEY` 미발급이라 라이브 응답을 보지 못했다.
`/api/debug/fish-market`이 키가 붙는 즉시 셋을 확정한다.

1. `csmtWt`가 kg인가 — 톤이면 원/kg가 1000배 어긋난다
2. `csmtUntpc`가 원/kg인가 — `unitPriceRatioMedian ≈ 1`
3. `mprcStdCodeNm`이 카탈로그 품목명과 매칭되는가

①이 틀려도 `FISH_PLAUSIBLE_PER_KG` 밴드가 거부하므로 화면에는 안 나간다.
③은 매칭 실패 시 품목이 숨겨질 뿐 틀린 값이 나가지 않는다. 둘 다 fail-safe다.

### 다음 할 일

1. **공공데이터포털 `DATA_GO_KR_SERVICE_KEY` 활용신청** (민호)
   — 이 키 하나로 aT 전국 도매시장 + 해수부 위판장을 함께 커버한다
2. 키 등록 후 `/api/debug/fish-market`으로 축 3종 확정 → `fishMarket.ts`
   "미검증" 주석 제거
3. 실제 `mprcStdCodeNm` 목록으로 `FISH_ALIASES` 정정
4. Chapter 1의 `DATABASE_URL` / `CRON_SECRET` 등록은 여전히 대기 중

---

## Chapter 3 — aT 전국 도매시장 원천 연결

*2026-07-31*

### 목표

청과 경락가를 가락 개별 API 하나에만 의존하고 있었다. aT 전국 공영도매시장
실시간 경매정보(15141808)를 붙여 1순위 원천으로 삼는다.

### 발견

`atMarket.ts`는 스캐폴딩만 있었고, `aggregateAtByItem`이 **단위를 무시한 채
원문 가격을 평균**내고 있었다. 10kg 상자와 20kg 상자가 한 평균에 들어가는,
Chapter 1에서 고친 것과 같은 종류의 축 결함이다. 어댑터를 다시 썼다.

### 축

**원/kg = 경락가 ÷ parseUnitKg(규격/단위), 거래량 가중평균.**

가락과 같은 방식이되 aT는 거래량이 있어 물량 가중이 된다.
중량으로 읽을 수 없는 행은 버리고 `droppedRows`로 센다.

### 소스 우선순위

청과: **aT → 가락 → KAMIS**

`resolveAuctionPerKg`을 3소스로 확장하고, 채택값과 다른 **어느 소스라도**
10배 이상 벌어지면 전부 버리도록 게이트를 넓혔다.

| 원천 | 담당 | 원/kg 얻는 법 | 추정 |
|---|---|---|---|
| KAMIS 도매 | 교차검증만 | 슬롯별 축 규칙 + 단위 파싱 | 필요 |
| aT 전국 도매시장 | 청과 1순위 | 경락가 ÷ 단위, 물량 가중 | 일부 |
| 가락 경매결과 | 청과 2순위 | `PPRICE ÷ UUN` | 일부 |
| 해수부 위판장 | 수산 | `csmtAmount ÷ csmtWt` | 없음 |

### ⚠ 필드명 미확정

15141808의 응답 스키마는 명세서가 **xlsx 첨부**로만 제공되어 웹에서 확인하지
못했다. 추측으로 하드코딩하는 대신 `AT_FIELD_CANDIDATES`에 문서로 확인된
후보들을 선언했다.

1. 농림축산식품 포털의 동일 성격 데이터에서 확인:
   `COST`(경락가) · `QTY`(물량) · `STD`(규격·단위) · `SMALLNAME` · `SANNAME` · `WHSALCD`
2. 15141808 페이지가 명시한 표준코드 필드:
   `whsl_mrkt_cd` · `corp_cd` · `unit_cd` · `gds_sclsf_cd` · `grd_cd` · `plor_cd`

매칭 실패는 행이 버려질 뿐 **틀린 값이 나가지 않는다.**
`/api/debug/at-market`이 응답 원시 키와 실제로 매칭된 키를 내려주므로,
키가 발급되면 그것으로 후보 목록을 좁힌다.

### 검증

- 테스트 **113개 통과** (15개 파일)
- 타입체크·린트·프로덕션 빌드 통과
- 키 없는 로컬 실행: 진단이 `missing_credentials` 명시,
  피드 items 0개(더미 없음), 홈·카테고리·상세 모두 200

### 다음 할 일

1. **`DATA_GO_KR_SERVICE_KEY` 활용신청** (민호) — 이 키 하나로
   aT 전국 도매시장 + 해수부 위판장이 동시에 열린다
2. `/api/debug/at-market`으로 필드명 확정 → `AT_FIELD_CANDIDATES` 좁히기
3. `/api/debug/fish-market`으로 위판장 축 3종 확정
4. 32개 시장 확대 여부 — 현재는 가락만 조회. "전국 평균"으로 바꿀지는
   제품 결정이 필요하다
5. Chapter 1의 `DATABASE_URL` / `CRON_SECRET` 등록은 여전히 대기 중

---

## Chapter 4 — 가격 비교 오류 리뷰와 데이터 인벤토리

*2026-07-31*

### 목표

지금 가진 API로 얻을 수 있는 정보를 정리하고, 상품목록을 손보고,
가격 비교에서 에러가 나는 상황을 찾는다.

### 찾은 것 — 원천 혼입

프로덕션에서 시금치 **+218%**, 감귤 +192%, 배 +178% 같은 전일대비가 찍히고
있었다. 시세 변동이 아니라 **원천 전환**이 등락률로 나온 값이다.

`prices.ts`가 오늘값은 가락/aT에서, 전일값은 KAMIS 시계열에서 가져왔다.
두 원천은 가격대가 다르다 — 배추 실측 가락 1,895 vs KAMIS 1,128.
**시세가 그대로여도 +68%가 찍힌다.**

같은 문제가 세 지표에 걸쳐 있었다.

| 지표 | 오늘값 | 비교 대상 | 결과 |
|---|---|---|---|
| changeRate | 가락 | KAMIS 전일 | 원천 차이가 등락률로 |
| deviationRate | 가락 | KAMIS 평년 | 전 품목에 상수 편차 |
| trendPercentile | 가락 | KAMIS 시계열 | 가락이 높아 항상 '고가권' |

Chapter 2에서 수산에는 같은 이유로 KAMIS를 분리했는데, **청과에는 적용하지
않았다.** 같은 클래스의 오류를 반만 고쳤던 셈이다.

### 수정

`PricePoint`에 `source`를 달고 **오늘값과 같은 원천의 시계열만** 비교에 쓴다.
근거가 없으면 지표를 만들지 않는다.

- `changeRate` / `deviationRate` / `auctionPrevPerKg` → optional
- `trendBasis`(`series`/`baseline`/`none`) 도입 → UI가 근거 없는 배지·분위·
  스파크라인을 감추고 "이력이 쌓이면 표시됩니다"로 대체
- `prevFromSeries`가 fallback으로 오늘값을 넣어 **"전일 0%"를 만들던 것 제거**.
  "변동 없음"과 "전일 데이터 없음"은 다른 사실이다
- DB 경로도 KAMIS 시계열 이어붙이기 제거, 상세 페이지도 동일 규칙 적용

### 수정 — 등급 불일치

가락은 특·상·중·하를 전부 평균내는데 KAMIS 소매는 '상품' 기준이었다.
도매만 낮게 잡혀 **거품배수가 구조적으로 과대평가**된다.
가락도 특·상 등급이 있으면 그것만 쓰도록 맞췄다.

### 상품목록 정리

- `PROCESSED_ITEM_IDS` 12건이 **전부 카탈로그에 없는 죽은 ID**였다.
  가공식품은 이미 제거됐는데 제외 목록만 남아 있었다.
  원천 품목명 기준 방어선(`PROCESSED_SOURCE_NAMES`)으로 교체 — KAMIS가 계속
  내려주므로 카탈로그 확장 시 재유입을 막는다.
- 추천·절약 후보에서 빠져 있던 일상 품목 11종 추가
  (생강·미나리·알배기배추·얼갈이배추·열무·피망·붉은고추·피마늘·레몬·전복·꼬막).
  카탈로그에 있는데 후보에서 빠진 품목이 **14 → 3**으로 줄었다.

### 품목 확장 후보 (가장 큰 누락)

KAMIS 부류는 6개인데 우리는 200/400/600 셋만 조회하고 있었다.

- **100 식량작물** — 감자·고구마는 가락 청과 거래 → 확장 후보
- **300 특용작물** — 버섯류(느타리·팽이·새송이)는 가락 거래 → 확장 후보
- 500 축산물 — 축산물도매시장 별도 체계 → 원천 없음

**감자·고구마·버섯은 장바구니 핵심인데 지금 아예 없다.**
`/api/debug/kamis-catalog?codes=100,300`으로 실제 품목명·단위를 확인한 뒤
추가한다 (KAMIS 키는 이미 살아 있으므로 배포만 되면 바로 가능).

### 문서

[`docs/DATA_INVENTORY.md`](docs/DATA_INVENTORY.md) 신설 — 원천별로 무엇이
들어오고 무엇을 쓰고 있는지, 아직 안 쓰는 정보(산지·품종·시장별 가격차),
그리고 **남아 있는 비교 한계** 4건을 정리했다.

### 남은 비교 한계 (수정 안 함, 문서화만)

1. **지역 불일치** — 경락은 가락 한 곳, 소매는 전국. aT가 열려야 맞출 수 있다
2. **유통 단계 불일치(수산)** — 산지 위판 vs 전국 소매라 단계가 하나 더 많다.
   카테고리별 거품 임계값이 필요하다
3. **소매 개수단위 왕복** — 카탈로그 중량이 틀리면 소매 원/kg도 틀어진다
4. **거품 임계값(1.8/2.5)이 축이 깨진 시기에 정해진 값** — 실측 분포로 재조정 필요

### 검증

- 테스트 **118개 통과** (15개 파일)
- 타입체크·린트·프로덕션 빌드 통과

### 다음 할 일

1. 배포 후 `/api/debug/kamis-catalog?codes=100,300` → 감자·고구마·버섯 추가
2. 산지(SSANGI) 노출 — 데이터는 이미 들어오고 있다
3. 축 정상화 후 카테고리별 거품 임계값 재조정
4. 키 대기: `DATA_GO_KR_SERVICE_KEY`(8/2 이후), `DATABASE_URL`/`CRON_SECRET`

*마지막 업데이트: 2026-07-31*

---

## Chapter 5 — 운영 안전장치와 법적 노출 차단

### 목표

사업 로드맵을 짜면서 세 팀(리서치·전략·비판)이 각각 짚은 것 중,
**코드로 지금 막을 수 있는 것**부터 닫는다. 마케팅을 시작하기 전에 해야 한다.

### 완료 내용

**1. 수집 실패 알림 — 조용한 실패 차단**

수집이 0건이어도 Cron이 HTTP 200을 돌려주고 있었다. Vercel은 성공으로 기록하고,
아무도 모르는 채 그날 경락가가 비었다. **지나간 시세는 다시 받을 수 없어
이 손실만 유일하게 복구가 불가능하다.**

판정을 두 갈래로 나눴다.

| 상황 | 판정 | HTTP |
|---|---|---|
| 예외(`failed`) | 첫날부터 `alert` | 502 |
| 빈손 1일 | `warn` (일요일 휴장 흡수) | 200 |
| 빈손 2일 연속 | `alert` | 502 |

공휴일 표를 하드코딩하지 않았다 — 그 표가 틀리면 알림이 거짓말을 한다.
연속 일수로 보면 달력을 몰라도 판정이 선다. 매주 울리는 알림은 곧 무시당하므로
이 절제가 알림의 수명을 결정한다.

- `assessIngest`/`emptyDayStreak`를 순수 함수로 분리, 테스트 11건
- 같은 날 재실행은 하루로 접고, 하나라도 성공하면 성공한 날로 본다
- `ALERT_WEBHOOK_URL`(선택) — 미설정이어도 런타임 로그에는 남는다
- `/api/health`에 `ingestHealth` 노출

**2. 개인정보 수집 동의 — 실재하던 위반**

대기자 폼이 이메일·관심 품목을 수집하면서 동의 절차도 고지도 없었다.
개인정보보호법 제15조·제30조 위반 소지. 트래픽 0일 땐 아무 일 없지만
화제가 되는 순간 신고 한 건으로 과태료 대상이 된다.

- 폼 체크박스 + **서버에서도 `consent !== true` 거부**
  (체크박스만으로는 API 직접 호출로 뚫리고, 동의 없이 들어온 레코드는
  사후 구제가 불가능하다)
- `/privacy` — 국외이전(Vercel 미국 / Supabase 일본)까지 숨기지 않고 명시

**3. `/policy` 가격 공개 원칙 3조**

아직 아무 이해관계가 없을 때 미리 적어뒀다. 나중에 쓰면 변명이 된다.

1. 시장 레벨 지표는 전면 공개 (어차피 공공 데이터)
2. 개별 사업자 견적·거래가는 영구 비공개, 표본 N<5 미표시
3. 소비자 영수증은 지역·업태 단위로만 집계

도매-소매 차이를 "거품"이라 부르지 않는 이유와, **경락가가 유통의 마지막
구간만 보여준다**는 한계도 함께 적었다.

**4. 산지가격 축 — 확보 실패, 대안 필요**

화면의 "소매 ÷ 경락" 중앙값은 **1.36배**(n=42)인데 국가 통계 유통비용률은
**49.2%**, 엽근채소는 **64.3%**다. 모순이 아니라 측정 구간이 다르다 —
소비자가를 100으로 놓으면 **경락가는 이미 63 지점**이고, 나머지는
산지·출하에서 결정된다. 우리는 마지막 한 토막만 보여주고 있었다.

전 구간을 보여주려면 축 앞쪽에 산지가격이 필요해 `/api/debug/origin-price`로
KAMIS 후보 action 4개를 실측했다.

| action | 결과 |
|---|---|
| `areaPriceList` / `originPriceList` / `farmPriceList` | HTTP 404 — 존재하지 않음 |
| `dailySalesList` | 200이나 HTML 반환 (API 아님) |
| `dailyPriceByCategoryList` (대조군) | 정상 — 키·네트워크 문제 아님 |

**결론: KAMIS에는 이 이름들로 산지가격 API가 없다.** 대조군이 살아 있으므로
자격증명 문제가 아니다. 대안은 aT 유통실태조사(연간 유통비용률) — 일별 시세가
아니라 구조 비율이지만, "이 품목은 농가가 몇 %를 받는다"를 보여주는 데는 충분하다.

### 검증

- 테스트 **139개 통과** (17개 파일, +21)
- 타입체크·린트·프로덕션 빌드 통과
- 프로덕션 실측: 동의 없는 요청 400, `ingestHealth` `warn`/`emptyStreak 1`,
  `/policy`·`/privacy` 200

### 다음 할 일

1. **8/2 18:00 이후** — `DATA_GO_KR_SERVICE_KEY` 활용신청 (aT 15141808,
   해수부 위판장 15056856) → Vercel 환경변수 등록 *(민호 직접)*
2. **8/3(월) 08:30** — 평일 경락가 해석률 실측. `/api/debug/price-axis`가
   `resolved 0/57`인 게 휴장 때문인지 가락 파싱 실패인지 판정한다.
   43품목 전부 `priceSource: kamis`인 것도 함께 확인 — AGENTS.md는 KAMIS
   도매를 교차검증 전용으로 못 박았는데 현재 주 원천으로 서빙되고 있다
3. 키 발급 후 aT 필드명 확정(`/api/debug/at-market`), 수산 축 확정
4. 실측 분포로 카테고리별 거품 임계값 재조정 (현 1.8/2.5는 축이 깨진 시기 값)
5. `ALERT_WEBHOOK_URL` 설정 — 미설정 시 런타임 로그에만 남는다 *(민호 직접)*
6. 산지 축 대안 조사 — aT 유통실태조사 원자료 또는 공공데이터포털 별도 API

*마지막 업데이트: 2026-08-01*
